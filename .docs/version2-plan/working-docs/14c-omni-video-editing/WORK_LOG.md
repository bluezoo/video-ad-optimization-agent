# Workstream 14c — omni-video-editing: WORK_LOG

## 2026-08-23 — checkpoint 1: kickoff (worktree created + phase doc research done)
Worktree `.claude/worktrees/version_2_omni-video-editing` created off `version_2` (b3285c3), branch `version_2_omni-video-editing`. Verified: `git merge-base HEAD version_2` == `git rev-parse version_2`.

No prior `00-overview.md` entry existed for this phase — net-new scope surfaced this session via a live-tested exploration (Workflow tool, 6 agents), prompted by the owner wanting to unlock demo CUJs 4 (real-time video edits) and 5 (EN→ES translation) that a prior capability audit found unimplemented. Phase doc `.docs/version2-plan/14c-omni-video-editing.md` written directly from that exploration's already-live-verified findings (not a fresh Step-2 research pass against a pre-existing doc, since none existed) — commit 35bfcf8, same commit amends `14b-video-model-upgrade-omni-flash.md` with the narrower-finding cross-reference (DISCOVERY, see below).

**DISCOVERY: 14b's Omni Flash NO-GO was narrower than its own text implied.** 14b (merged, PR #14, 2026-07-23) evaluated `previous_interaction_id`-based chained revision against `text_to_video`/`image_to_video` tasks and issued a blanket-reading NO-GO ("steps 4-6 deferred"). This session's live exploration tested a different, simpler mechanic — a single-shot `edit`-task call against an *already-generated* video, no `previous_interaction_id` — and it worked: verified end-to-end on Vertex AI (`gemini-omni-flash-preview`, `google-genai` 2.19.0 in an isolated venv; repo pins 2.12.1, below the ≥2.14.0 working floor), confirmed by frame-level before/after comparison (only the targeted attribute — a garment color — changed; face/hair/other garments/background/pose/lighting were pixel-stable). Two undocumented API contract details were found live and are now recorded in the phase doc: `VideoContent.data` needs `pathlib.Path(...)`, not a bare string (else a corrupt-base64 400); `response_format.aspect_ratio` must be omitted for edit tasks (else a 400). The same exploration also tested EN→ES dialogue translation via the identical mechanism — the call succeeded transport-wise but produced a code-switched Spanglish result with lips independently judged unsynced, consistent with Google's own docs ("Voice editing is not supported") — confirmed broken, not merely untested.

Blast radius amended with provenance in this branch: `14b-video-model-upgrade-omni-flash.md` (narrower-finding cross-reference note, commit 35bfcf8); `14c-omni-video-editing.md` itself carries the full discovery as its own "Relationship to Phase 14b" section. No other phase docs reference Omni Flash, so no further sweep needed.

`STATUS.md` (main checkout) registered `14c` as `kickoff in progress`, commit c13125c on `version_2` (pushed) — done from the main checkout per the single-writer rule, since this worktree-isolated session cannot edit files outside its own worktree path directly.

Owner directive for this workstream (verbatim intent): translation/dubbing is explicitly deferred ("the audio translation we can work later"); build the editing feature "real quick and test in the workstream" — i.e. narrow scope (visual edit tool only), move efficiently through the lifecycle, but the demo-scenario verification step is not to be skipped.

## 2026-08-23 — checkpoint 2: working doc approved
Owner approved the six-dimension restatement as-is ("Yes, proceed"): opt-in edit_video_with_omni tool on the existing Review Agent, gated by ENABLE_OMNI_EDIT (default off); google-genai bump to >=2.14.0 as its own isolated step; unit (network-free) + live-tier + demo-scenario verification; translation/dubbing and chained/multi-attribute edits stay explicitly out of scope.

## 2026-08-23 — checkpoint 3: plan approved
Plan saved to working-docs/14c-omni-video-editing/plan.md (3 tasks: SDK bump + config + DB lineage column; video_edit_tools.py module + network-free unit tests; Review Agent wiring + live test + demo scenario + docs). Owner's earlier "Yes, proceed" explicitly covered "write the plan now and start subagent-driven-development for this exact scope" — treated as covering plan approval too, not re-asked separately, per the owner's "real quick" framing.

## 2026-08-23 — Task 1 complete (mirrors .superpowers/sdd/progress.md)
google-genai bumped to >=2.14.0 (worktree installed 2.19.0); OMNI_EDIT_MODEL + ENABLE_OMNI_EDIT added to app/config.py; source_video_id lineage column added to campaign_videos (fresh CREATE TABLE + run_migrations() migration block, both directions verified by the reviewer). commit 045bdf0, review clean, 433 tests green (408 unit + 25 e2e).

## 2026-08-23 — Task 2 complete (mirrors .superpowers/sdd/progress.md)
video_edit_tools.py: edit_video_with_omni + translate_video_dialogue placeholder + 8 network-free unit tests (every call site mocks _build_client). commit 58be26c, review clean, 441 tests green (416 unit + 25 e2e).

DISCOVERY (implementation-level, no blast radius beyond this task): the plan's Task 2 test brief assumed VideoContent's constructed `.data` attribute would still be a `pathlib.Path` after construction. Verified against the installed google-genai 2.19.0 SDK source: `VideoContent.data` is typed with a pydantic `BeforeValidator` that unconditionally coerces the constructor argument to base64 `str` before validation completes — so `.data` is always `str` post-construction regardless of what was passed in. Fixed by spying on the `VideoContent` constructor call itself (captures the pre-validation kwarg type) rather than reading the built instance back — reviewer independently verified this still fails when the call site is regressed to a bare str. No change to the module's actual behavior or the phase doc's contract-detail #1 (the call site still must pass a Path); this only affects how the regression test proves it.

## 2026-08-23 — Task 3 complete (mirrors .superpowers/sdd/progress.md)
edit_video_with_omni registered on review_agent gated by ENABLE_OMNI_EDIT (commit 3172c15, flag-off regression test verified byte-identical tool list via importlib.reload); live test against real Vertex AI PASSED (commit c1ccb9a) -- real evidence: interaction id video-8b272ec1-b9bd-47b2-b4ea-1423c38bc4a5, source_video_id=1 -> new_video_id=11, 83.59s; demo scenario F7 (fashion.md) + DEMO_GUIDE "Workstream 14c" journeys added (commit f97633f). Review approved all three commits.
Note: this fresh worktree had no app/.env or generated/ assets; implementer copied app/.env from this repo's own main checkout (standard worktree practice) and one seeded demo video (blue-floral-maxi-dress-122025-asian-beach-romantic.mp4, the literal first REAL_VIDEOS entry in mock_data.py) from a differently-named local sibling directory's demo-assets cache, purely to unblock the live test -- reviewer independently confirmed both are fully gitignored with zero footprint in any commit, judged low-risk standard practice, not requiring escalation.

## 2026-08-23 — checkpoint 5: demo-scenario verification PASS
Scenario F7 (docs/demo-scenarios/fashion.md), both scenes: 2/2 PASS.
Scene F7.1 (ENABLE_OMNI_EDIT=true): edit_video_with_omni(video_id=1, ...) fired via review_agent, real Vertex/Omni call succeeded (interaction_id=video-4f2efa8d-..., source_video_id=1 -> new_video_id=11), DB assertion confirmed (`SELECT source_video_id FROM campaign_videos WHERE id=11` -> 1), chat response referenced both video ids.
Scene F7.2 (flag off, default): edit_video_with_omni absent from the full 18-event trace and from the review_agent tool-declaration block; agent correctly fell back to Media Agent's standard two-stage Veo pipeline, response did not misrepresent a visual edit as having occurred. (One transient Vertex 429 on the first attempt, unrelated to the feature under test, resolved on retry per the verifier's one-retry allowance.)
Evidence: .superpowers/sdd/verify-evidence-omni-edit/ (screenshots + trace snapshot; gitignored scratch, not committed -- same convention as prior workstreams' verify-evidence dirs).

## 2026-08-23 — final whole-branch review + fixes
Dispatched final whole-branch review (opus) over b3285c3..d57b946. Verdict: "Ready to merge: With fixes." No Critical findings. 5 Important: (1) stale `google-genai>=1.55.0` floor in `scripts/deploy_ae_inline.py` not bumped alongside `app/requirements.txt`; (2) raw exceptions (FileNotFoundError/GCS NotFound, binascii.Error, save/insert failures) could leak past `edit_video_with_omni`'s error-dict contract on realistic failure paths; (3) `error_type` taxonomy mapped `cancelled`/`budget_exceeded` to the misleading `unsupported_edit`; (4) ENABLE_OMNI_EDIT=true agent-wiring case had no unit test (only flag-off was covered); (5) SETUP_INSTRUCTIONS.md's accumulating version-2 setup-notes section wasn't updated with the new env vars/SDK floor/live-test asset prerequisite.
All 5 fixed directly (commit 9dd1c89): deploy floor bumped to >=2.14.0; storage/base64/persist paths now wrapped and mapped to a new `storage_error` error_type; cancelled/budget_exceeded now map to `api_error`; added `test_review_agent_tools_include_omni_edit_when_enabled` (flag-on mirror) + two new video_edit_tools tests (missing-file, cancelled-status); SETUP_INSTRUCTIONS.md updated with the workstream 14c setup-notes entry and a flakiness-watchlist addendum; live test's `seeded_video_id` fixture now `pytest.skip`s (matching test_media_judge.py's pattern) instead of failing when the demo asset is absent. Also fixed two Minor items: `local_path` now persisted on the new row (thumbnail rendering) and the migration ALTER's FK clause now matches the fresh CREATE TABLE's `ON DELETE SET NULL`.
Discoveries-protocol amendment (commit 5271419): folded 14c into `00-overview.md`'s phase table and added `99-open-questions.md` Q20 (edit chaining, multi-attribute, GCS support -- all explicitly out of scope for this workstream) plus an amendment to Q15 recording the narrower single-shot-edit finding at that doc's own location.
Full suite re-verified green after fixes: `make lint` clean, `make test` 417 unit passed/1 skipped + 25 e2e passed/1 skipped, new/modified test files individually re-run (16/16 passed).

## 2026-08-23 — owner-directed addition: AGENT_MODEL bump to gemini-3.7-flash
Bundled into this workstream at the owner's explicit direction (not part of the original approved scope) rather than spun out as its own workstream, given the small size of the change (config.py default only, GA/global-region contract unaffected). commit a08083f: `app/config.py` MODEL default gemini-3.6-flash -> gemini-3.7-flash, matching `tests/unit/test_config.py` assertion, comment updates in `tests/live/judge.py`/`scripts/deploy_ae_inline.py`, `DEMO_GUIDE.md` Journey 14.1 expected value, new `SETUP_INSTRUCTIONS.md` bullet (kept separate from the Phase 14a/14b historical bullet). Full suite re-verified green (`make lint`, `make test`).
